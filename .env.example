# ── Admin initial (seed DB) ────────────────────────────────────────
# Si la table `users` est vide au démarrage, un compte admin est créé avec ces identifiants.
# Ensuite la gestion des comptes se fait dans l’app (menu Comptes, réservé admin).
ADMIN_USER=admin
ADMIN_PASSWORD=changeme

# Secret de signature des cookies de session (obligatoire en prod)
SESSION_SECRET=change-me-to-a-long-random-string

# Compat : anciennes variables encore lues si ADMIN_* absentes
# AUTH_USER=admin
# AUTH_PASS=changeme

# Server
PORT=3001
# DEBUG global backend (HTTP + erreurs + DB)
# BACKEND_DEBUG=1
# Ou séparément :
# DEBUG_HTTP=1
# DEBUG_ERRORS=1
# DB_DEBUG=1

# ── Base MySQL (optionnel — sinon SQLite sous DATA_DIR) ───────────
# Si DATABASE_URL est défini, OU si MYSQL_HOST + MYSQL_USER + MYSQL_PASSWORD + MYSQL_DATABASE le sont,
# le backend utilise mysql2 au lieu de better-sqlite3.
# Exemple Hostinger / PlanetScale style :
# DATABASE_URL=mysql://user:password@host:3306/nom_base
# MYSQL_HOST=localhost
# MYSQL_PORT=3306
# MYSQL_USER=
# MYSQL_PASSWORD=
# MYSQL_DATABASE=

# ── Persistance des données (production) ─────────────────────────
# Dossier RACINE : la base SQLite et le sous-dossier uploads/ y sont créés
# (fichiers HTTP /uploads/xxx → <DATA_DIR>/uploads/xxx).
# Relatif au dossier backend/ si chemin non absolu. Défaut si absent : backend/data
# Si tu mets par erreur DATA_DIR=data/uploads, le backend remonte à data/ pour éviter uploads/uploads.
# DATA_DIR=data
# DATA_DIR=/var/lib/card-designer/data
# Sur hébergeur à disque éphémère : monter un volume persistant sur ce chemin.
